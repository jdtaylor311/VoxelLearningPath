#include "hello_utils.hpp"

//TEMP stubs to make tests compile. They should FAIL at first.
//Replace with real logic after you run tests

int count_letters(std::string& /*name*/) {
	return -1;
}

std::string format_greeting(const std::string& name) {
	return "FIXME";
}